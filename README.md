# Hadar Icons VS Code

This is the icon theme extension for Hadar.

## Features

- Custom file and folder icons
- Dynamic icon generation

## Development

1. Run `npm install`
2. Run `npm run build` to generate the icon theme JSON.
3. Press F5 to debug.
